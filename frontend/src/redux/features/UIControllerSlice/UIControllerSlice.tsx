import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const UIControllerSlice: any = createSlice({
  name: "UIController",
  initialState,
  reducers: {},
});

export const {} = UIControllerSlice.actions;

export default UIControllerSlice.reducer;
